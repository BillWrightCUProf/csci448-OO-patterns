package testquestions.observer.question;

//Write the code to make this class an observer of Polymorphia and show, in code,
// how it would be attached. The MazeViewer should NOT be coupled directly to Polymorphia.
// Define any extra interfaces that you need. Write only what is necessary to define
// the connections. In most cases this will just be signatures. Do not modify EventObserver
// or EventIssuingObservable.
public class MazeViewer {
    public void show(String mazeDescription) {
    }
}
